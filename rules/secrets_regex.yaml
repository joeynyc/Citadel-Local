# Starter rules for secrets scanning
# Extend with your own org-specific patterns.
patterns:
  - id: secrets.aws_access_key_id
    regex: "\bAKIA[0-9A-Z]{16}\b"
    severity: high
  - id: secrets.private_key_block
    regex: "-----BEGIN (RSA|EC|OPENSSH) PRIVATE KEY-----"
    severity: critical
